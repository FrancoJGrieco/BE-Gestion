const { errReturn } = require("./errReturn");
const { numVerification } = require("./numVerification");
const { noSuccess } = require("./noSuccess");

module.exports = {
	errReturn,
	noSuccess,
	numVerification,
};
